#source 'https://gitee.com/topstep/podspecs.git'
source 'https://github.com/CocoaPods/Specs.git'  # 官方源

platform :ios, '15.0'

use_frameworks! :linkage => :static

# 预编译 SDK 的二进制框架按动态框架链接了以下传递依赖
# （运行时 @rpath 加载），故强制这些传递依赖编为动态框架并嵌入 App，
# 否则启动时 dyld 会因找不到对应 framework 而崩溃。
# TopStepToolKit/AIBudsCore 依赖 zipzap，需同步动态构建以满足传递依赖检查。
dynamic_frameworks = ['AFNetworking', 'Protobuf', 'SocketRocket', 'SSZipArchive', 'TopStepToolKit', 'zipzap']
pre_install do |installer|
  installer.pod_targets.each do |pod|
    if dynamic_frameworks.include?(pod.name)
      def pod.static_framework?; false; end
      def pod.build_type; Pod::BuildType.dynamic_framework; end
    end
  end
end


target 'TopStepComKit-Git_Example' do

  # FitCloudKit / RTK 系列全部由 FitImp/Base 下的本地 xcframework 提供，不走 CocoaPods。
  # 拓步智能手表 SDK
#  pod 'FitCloudKit', git: 'https://github.com/htangsmart/FitCloudPro-SDK-iOS.git'
#  # 拓步能手环固件升级 SDK
#  pod 'FitCloudDFUKit', git: 'https://github.com/htangsmart/FitCloudPro-SDK-iOS.git'
#  # 拓步能手表表盘 SDK 适用于 Realtek 芯片
#  pod 'FitCloudWFKit', git: 'https://github.com/htangsmart/FitCloudPro-SDK-iOS.git'
#  # 拓步能手表表盘 SDK 适用于 Nordic 芯片
#  pod 'FitCloudNWFKit', git: 'https://github.com/htangsmart/FitCloudPro-SDK-iOS.git'

  


#pod 'TopStepComKit', :path => '/Users/panshi/Desktop/Project/NewSDK/TopStepComKit/TopStepComKit'
#pod 'TopStepFitKit/AI', :path => '/Users/panshi/Desktop/Project/NewSDK/TopStepComKit/TopStepFitKit'
#pod 'TopStepAIKit', :path => '/Users/panshi/Desktop/Project/NewSDK/TopStepComKit/TopStepAIKit'
## Persimwear(Fw) 暂不接入：persimwearSDK.framework 与 TopStepAIKit(AIBuds) 内置的 openssl.framework
## 存在 1200+ 个重复 C 符号（静态链接下 ld 报 duplicate symbols），且 Demo 源码未引用该平台。
##pod 'TopStepPersimwearKit/AI', :path => '/Users/panshi/Desktop/Project/NewSDK/TopStepComKit/TopStepPersimwearKit'
#pod 'TopStepNewPlatformKit/AI', :path => '/Users/panshi/Desktop/Project/NewSDK/TopStepComKit/TopStepNewPlatformKit'
#pod 'TopStepBleMetaKit', :path => '/Users/panshi/Desktop/Project/NewSDK/TopStepComKit/TopStepBleMetaKit'
#pod 'TopStepInterfaceKit', :path => '/Users/panshi/Desktop/Project/NewSDK/TopStepComKit/TopStepInterfaceKit'
#pod 'TopStepToolKit', :path => '/Users/panshi/Desktop/Project/NewSDK/TopStepComKit/TopStepToolKit'

#  local framework
#topstep_comkit_path = '..'
#pod 'TopStepComKit-Git/Interface', :path => topstep_comkit_path
#pod 'TopStepComKit-Git/ToolsAll', :path => topstep_comkit_path
#pod 'TopStepComKit-Git/ComKit', :path => topstep_comkit_path
#pod 'TopStepComKit-Git/AIImp', :path => topstep_comkit_path
## Fit + AI
#pod 'TopStepComKit-Git/FitAIImp', :path => topstep_comkit_path
## NPK
#pod 'TopStepComKit-Git/NpkImp', :path => topstep_comkit_path


#  remote branch
#pod 'TopStepComKit', :git => 'https://gitee.com/topstep/TopStepComKit.git', :branch => 'develop'
#pod 'TopStepFitKit/AI', :git => 'https://gitee.com/topstep/TopStepFitKit.git', :branch => 'develop'
#pod 'TopStepAIKit', :git => 'https://gitee.com/topstep/TopStepAIKit.git', :branch => 'develop'
#pod 'TopStepNewPlatformKit', :git => 'https://gitee.com/topstep/TopStepNewPlatformKit.git', :branch => 'develop'
#pod 'TopStepBleMetaKit', :git => 'https://gitee.com/topstep/TopStepBleMetaKit.git', :branch => 'develop'
#pod 'TopStepInterfaceKit', :git => 'https://gitee.com/topstep/TopStepInterfaceKit.git', :branch => 'develop'
#pod 'TopStepPersimwearKit', :git => 'https://gitee.com/topstep/TopStepPersimwearKit.git', :branch => 'develop'
#pod 'TopStepToolKit', :git => 'https://gitee.com/topstep/TopStepToolKit.git', :branch => 'develop'


#  pod 'TopStepComKit-Git',
#      :git => 'https://github.com/htangsmart/TopStepComKit.git',
#      :branch => 'develop',
#      :subspecs => ['ComKit','AIImp','FitAIImp','NpkImp']

#  release
  pod 'TopStepComKit-Git', :git => 'https://github.com/htangsmart/TopStepComKit.git', :tag => '1.0.0-beta11'

  
#    pod 'TopStepComKit-Git/ComKit',:git => 'https://github.com/htangsmart/TopStepComKit.git', :tag => '1.0.0-beta10'
#    pod 'TopStepComKit-Git/NpkImp', :git => 'https://github.com/htangsmart/TopStepComKit.git', :tag => '1.0.0-beta10'
#    pod 'TopStepComKit-Git/AIImp',:git => 'https://github.com/htangsmart/TopStepComKit.git', :tag => '1.0.0-beta10'
#
  target 'TopStepComKit-Git_Tests' do
    inherit! :search_paths
  end
end

# TopStepFitKit 仍需写入 TSDialTime 的内部可写属性；仅在编译该 Pod 时补充兼容声明。
fit_compatibility_header = '$(PODS_ROOT)/../BuildCompatibility/TSDialTime+TopStepFitKitCompatibility.h'

# AIBudsAI 预编译库依赖 WCDBSwift 的稳定 Swift ABI 符号。
post_install do |installer|
  # AFNetworking 4.0.1 直接引用了私有头 <netinet6/in6.h>，iOS 27 SDK 将其视为模块私有头而报错。
  # <netinet/in.h> 已包含其内容，这里在每次 pod install 后移除该行。
  Dir.glob(File.join(installer.sandbox.root, 'AFNetworking', 'AFNetworking', '*.m')).each do |file|
    content = File.read(file)
    next unless content.include?('#import <netinet6/in6.h>')
    File.chmod(0644, file)
    File.write(file, content.gsub("#import <netinet6/in6.h>\n", ''))
  end

  installer.pods_project.targets.each do |target|
    # Xcode 27 / iOS 27 SDK 仅支持 15.0 及以上的部署版本，统一抬高低于 15.0 的 Pod。
    target.build_configurations.each do |configuration|
      current = configuration.build_settings['IPHONEOS_DEPLOYMENT_TARGET']
      if current.nil? || Gem::Version.new(current) < Gem::Version.new('15.0')
        configuration.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'
      end
    end

    if target.name == 'WCDB.swift'
      target.build_configurations.each do |configuration|
        configuration.build_settings['BUILD_LIBRARY_FOR_DISTRIBUTION'] = 'YES'
      end
    end

    if target.name == 'TopStepFitKit'
      target.build_configurations.each do |configuration|
        other_cflags = Array(configuration.build_settings['OTHER_CFLAGS'] || '$(inherited)')
        configuration.build_settings['OTHER_CFLAGS'] = other_cflags + ['-include', fit_compatibility_header]
      end
    end
  end
end
